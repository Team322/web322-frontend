const Dashboard = () => (
    <div className="flex flex-col items-center justify-center h-screen">
        <h1 className="text-4xl font-bold mb-8">You are at the dashboard</h1>
    </div>
)

export default Dashboard;